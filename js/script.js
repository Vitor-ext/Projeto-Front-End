function Peq (event) {
    event.addEventListener();

    let busca = document.querySelector('pesquisa').value;

}










window.onload = () => {
    const pesquisa = document.querySelector('pesquisa');
    pesquisa.addEventListener('click', Peq);

}